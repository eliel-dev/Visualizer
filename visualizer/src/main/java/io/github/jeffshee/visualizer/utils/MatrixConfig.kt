package io.github.jeffshee.visualizer.utils

object MatrixConfig {
    // Número de LEDs na horizontal (colunas) e vertical (linhas)
    var ledMatrixColumns: Int = 64  // alterado de 16
    var ledMatrixRows: Int = 32     // alterado de 8
}
